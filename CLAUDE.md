# Claude Code instructions

This repo's agent instructions live in `AGENTS.md` (the convention other coding
CLIs read). Claude Code does not load that file automatically — read it now,
at the start of every session, and follow it. Keep `AGENTS.md` and this file's
pointer to it in sync if either changes.
